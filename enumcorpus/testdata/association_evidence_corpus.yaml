# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... (or go run ./cmd/gen-enum-corpora).
# Source of truth: schema.AllAssociationEvidences. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: AssociationEvidence commit_and_touch is valid
      input: commit_and_touch
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationEvidences
      mutation:
        description: removing commit_and_touch from schema.AllAssociationEvidences would reject a value the wire still accepts.
    - name: AssociationEvidence commit_only is valid
      input: commit_only
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationEvidences
      mutation:
        description: removing commit_only from schema.AllAssociationEvidences would reject a value the wire still accepts.
    - name: AssociationEvidence touch_only is valid
      input: touch_only
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationEvidences
      mutation:
        description: removing touch_only from schema.AllAssociationEvidences would reject a value the wire still accepts.
    - name: AssociationEvidence branch_only is valid
      input: branch_only
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationEvidences
      mutation:
        description: removing branch_only from schema.AllAssociationEvidences would reject a value the wire still accepts.
    - name: AssociationEvidence time_window is valid
      input: time_window
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationEvidences
      mutation:
        description: removing time_window from schema.AllAssociationEvidences would reject a value the wire still accepts.
    - name: AssociationEvidence outside the closed set is rejected
      input: unknown-evidence
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllAssociationEvidences
      mutation:
        description: accepting "unknown-evidence" would silently widen the closed association-evidence set beyond AllAssociationEvidences.
