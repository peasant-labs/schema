# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... (or go run ./cmd/gen-enum-corpora).
# Source of truth: schema.AllAssociationKinds. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: AssociationKind bound is valid
      input: bound
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationKinds
      mutation:
        description: removing bound from schema.AllAssociationKinds would reject a value the wire still accepts.
    - name: AssociationKind candidate is valid
      input: candidate
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationKinds
      mutation:
        description: removing candidate from schema.AllAssociationKinds would reject a value the wire still accepts.
    - name: AssociationKind temporal is valid
      input: temporal
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllAssociationKinds
      mutation:
        description: removing temporal from schema.AllAssociationKinds would reject a value the wire still accepts.
    - name: AssociationKind outside the closed set is rejected
      input: unknown-kind
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllAssociationKinds
      mutation:
        description: accepting "unknown-kind" would silently widen the closed association-kind set beyond AllAssociationKinds.
