# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... (or go run ./cmd/gen-enum-corpora).
# Source of truth: schema.AllConfidences. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: Confidence high is valid
      input: high
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllConfidences
      mutation:
        description: removing high from schema.AllConfidences would reject a value the wire still accepts.
    - name: Confidence medium is valid
      input: medium
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllConfidences
      mutation:
        description: removing medium from schema.AllConfidences would reject a value the wire still accepts.
    - name: Confidence low is valid
      input: low
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllConfidences
      mutation:
        description: removing low from schema.AllConfidences would reject a value the wire still accepts.
    - name: Confidence outside the closed set is rejected
      input: unknown-confidence
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllConfidences
      mutation:
        description: accepting "unknown-confidence" would silently widen the closed confidence set beyond AllConfidences.
