# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... (or go run ./cmd/gen-enum-corpora).
# Source of truth: schema.AllInsightKinds. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: InsightKind decision is valid
      input: decision
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllInsightKinds
      mutation:
        description: removing decision from schema.AllInsightKinds would reject a value the wire still accepts.
    - name: InsightKind friction is valid
      input: friction
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllInsightKinds
      mutation:
        description: removing friction from schema.AllInsightKinds would reject a value the wire still accepts.
    - name: InsightKind unusual is valid
      input: unusual
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllInsightKinds
      mutation:
        description: removing unusual from schema.AllInsightKinds would reject a value the wire still accepts.
    - name: InsightKind retry_loop is valid
      input: retry_loop
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllInsightKinds
      mutation:
        description: removing retry_loop from schema.AllInsightKinds would reject a value the wire still accepts.
    - name: InsightKind outside the closed set is rejected
      input: unknown-kind
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllInsightKinds
      mutation:
        description: accepting "unknown-kind" would silently widen the closed insight-kind set beyond AllInsightKinds.
