# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... from the module root.
# Source of truth: schema.AllInsightProvenances. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: InsightProvenance mechanical is valid
      input: mechanical
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllInsightProvenances
      mutation:
        description: removing mechanical from schema.AllInsightProvenances would reject a value the wire still accepts.
    - name: InsightProvenance mined is valid
      input: mined
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllInsightProvenances
      mutation:
        description: removing mined from schema.AllInsightProvenances would reject a value the wire still accepts.
    - name: InsightProvenance outside the closed set is rejected
      input: unknown-provenance
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllInsightProvenances
      mutation:
        description: accepting "unknown-provenance" would silently widen the closed insight-provenance set beyond AllInsightProvenances.
