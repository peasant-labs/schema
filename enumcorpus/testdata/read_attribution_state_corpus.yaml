# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... from the module root.
# Source of truth: schema.AllReadAttributionStates. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: ReadAttributionState complete is valid
      input: complete
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllReadAttributionStates
      mutation:
        description: removing complete from schema.AllReadAttributionStates would reject a value the wire still accepts.
    - name: ReadAttributionState partial is valid
      input: partial
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllReadAttributionStates
      mutation:
        description: removing partial from schema.AllReadAttributionStates would reject a value the wire still accepts.
    - name: ReadAttributionState unavailable is valid
      input: unavailable
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllReadAttributionStates
      mutation:
        description: removing unavailable from schema.AllReadAttributionStates would reject a value the wire still accepts.
    - name: ReadAttributionState outside the closed set is rejected
      input: unknown-state
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllReadAttributionStates
      mutation:
        description: accepting "unknown-state" would silently widen the closed read-attribution-state set beyond AllReadAttributionStates.
