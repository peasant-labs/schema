# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... (or go run ./cmd/gen-enum-corpora).
# Source of truth: schema.AllRewriteMethods. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: RewriteMethod hash is valid
      input: hash
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: removing hash from schema.AllRewriteMethods would reject a value the wire still accepts.
    - name: RewriteMethod patch_id is valid
      input: patch_id
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: removing patch_id from schema.AllRewriteMethods would reject a value the wire still accepts.
    - name: RewriteMethod author_identity is valid
      input: author_identity
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: removing author_identity from schema.AllRewriteMethods would reject a value the wire still accepts.
    - name: RewriteMethod message_embedded is valid
      input: message_embedded
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: removing message_embedded from schema.AllRewriteMethods would reject a value the wire still accepts.
    - name: RewriteMethod temporal is valid
      input: temporal
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: removing temporal from schema.AllRewriteMethods would reject a value the wire still accepts.
    - name: RewriteMethod none is valid
      input: none
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: removing none from schema.AllRewriteMethods would reject a value the wire still accepts.
    - name: RewriteMethod outside the closed set is rejected
      input: unknown-method
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllRewriteMethods
      mutation:
        description: accepting "unknown-method" would silently widen the closed rewrite-method set beyond AllRewriteMethods.
