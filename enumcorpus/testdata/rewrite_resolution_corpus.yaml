# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... (or go run ./cmd/gen-enum-corpora).
# Source of truth: schema.AllRewriteResolutions. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: RewriteResolution live is valid
      input: live
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteResolutions
      mutation:
        description: removing live from schema.AllRewriteResolutions would reject a value the wire still accepts.
    - name: RewriteResolution rewritten is valid
      input: rewritten
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteResolutions
      mutation:
        description: removing rewritten from schema.AllRewriteResolutions would reject a value the wire still accepts.
    - name: RewriteResolution unresolved is valid
      input: unresolved
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllRewriteResolutions
      mutation:
        description: removing unresolved from schema.AllRewriteResolutions would reject a value the wire still accepts.
    - name: RewriteResolution outside the closed set is rejected
      input: unknown-resolution
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllRewriteResolutions
      mutation:
        description: accepting "unknown-resolution" would silently widen the closed rewrite-resolution set beyond AllRewriteResolutions.
