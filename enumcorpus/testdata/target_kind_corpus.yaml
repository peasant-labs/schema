# Code generated by cmd/gen-enum-corpora; DO NOT EDIT.
# Regenerate: go generate ./... from the module root.
# Source of truth: schema.AllTargetKinds. Widening the set without
# regenerating this corpus reddens the exhaustive-coverage test.
cases:
    - name: TargetKind session is valid
      input: session
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllTargetKinds
      mutation:
        description: removing session from schema.AllTargetKinds would reject a value the wire still accepts.
    - name: TargetKind entry is valid
      input: entry
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllTargetKinds
      mutation:
        description: removing entry from schema.AllTargetKinds would reject a value the wire still accepts.
    - name: TargetKind annotation is valid
      input: annotation
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllTargetKinds
      mutation:
        description: removing annotation from schema.AllTargetKinds would reject a value the wire still accepts.
    - name: TargetKind project is valid
      input: project
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllTargetKinds
      mutation:
        description: removing project from schema.AllTargetKinds would reject a value the wire still accepts.
    - name: TargetKind file_version is valid
      input: file_version
      expected: true
      classification: must-pass
      provenance:
        source: enum
        ref: schema.AllTargetKinds
      mutation:
        description: removing file_version from schema.AllTargetKinds would reject a value the wire still accepts.
    - name: TargetKind outside the closed set is rejected
      input: unknown-target
      expected: false
      classification: must-fail
      provenance:
        source: enum
        ref: schema.AllTargetKinds
      mutation:
        description: accepting "unknown-target" would silently widen the closed target-kind set beyond AllTargetKinds.
